//The URL Module breaks/splits a web address into readable parts.
//It provides utilities for URL parsing and resolution

const newUrl = new URL("https://jsonplaceholder.typicode.com");
console.log(newUrl);

//The URL method accepts two argument: the url and options. Logging the contents of the method to the console gives a JSON that has all the properties of the url.
